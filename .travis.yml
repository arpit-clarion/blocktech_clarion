language: ruby
rvm:
  - "2.1.0"
env:
  - DB=sqlite
script: 
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec rspec spec/
before_script:
  - sqlite -e 'create database myapp_test'
bundler_args: --binstubs=./bundler_stubs